{
	"name": "create-robin-app",
	"version": "0.0.0",
	"description": "Create an app for the Robin platform.",
	"main": "index.mjs",
	"license": "GPL-3.0",
	"files": [
		"LICENSE",
		"README.md",
		"index.js"
	],
	"scripts": {
		"format": "prettier --write src",
		"lint": "npm-run-all -p lint:*",
		"lint:tsc": "tsc",
		"lint:prettier": "prettier --check src",
		"build": "esbuild --platform=node --target=node16 --outfile=index.mjs src/index.ts"
	},
	"devDependencies": {
		"@types/prompts": "2.4.2",
		"esbuild": "0.17.10",
		"typescript": "4.9.5"
	},
	"dependencies": {
		"prompts": "2.4.2",
		"validate-npm-package-name": "5.0.0"
	}
}
