{
	"name": "@robinplatform/backend",
	"version": "0.0.0",
	"license": "GPL-3.0",
	"bin": {
		"robin": "./bin/cli"
	},
	"scripts": {
		"dev": "go run github.com/mitranim/gow run ./cmd/cli start",
		"format": "go fmt ./... && go vet ./...",
		"build": "yarn build-cli",
		"go-generate": "go generate ./...",
		"build-cli": "go build -o ./bin/cli -tags prod ./cmd/cli"
	},
	"devDependencies": {}
}
